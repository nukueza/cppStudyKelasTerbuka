#include <iostream>
#include <string>

using std::cout;
using std::endl;
using std::string;

int main (int argc, char *argv[]) {

  int num = 0;
  
  cout << (num == 0 ? "yes" : "no") << endl;

  return 0;
}
